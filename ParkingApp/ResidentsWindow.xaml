<Window x:Class="ParkingApp.ResidentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rezidentlarni boshqarish" Height="500" Width="869"
        Background="#0F172A">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#F1F5F9"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="CaretBrush" Value="#F1F5F9"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#94A3B8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="139*"/>
            <RowDefinition Height="85*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="F.I.Sh:"/>
            <TextBox x:Name="FullNameBox" Width="806" RenderTransformOrigin="0.503,0.573"/>

            <TextBlock Text="Xonadon raqami:"/>
            <TextBox x:Name="ApartmentBox" Margin="0,2,0,8" Width="805"/>

            <TextBlock Text="Mashina raqami:"/>
            <TextBox x:Name="PlateBox" Margin="0,2,0,8" Width="806"/>

            <Button Content="Qo'shish" Click="AddButton_Click" Margin="0,5,0,0"
                    Width="100" HorizontalAlignment="Left" Background="#10B981"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="RO'YXAT : " FontWeight="Bold"
                    Foreground="#F1F5F9" HorizontalAlignment="Center"/>

        <DataGrid x:Name="ResidentsDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="#1E293B" Foreground="#F1F5F9"
                  BorderBrush="#334155" BorderThickness="1"
                  RowBackground="#1E293B" AlternatingRowBackground="#243248"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#334155" SelectionChanged="ResidentsDataGrid_SelectionChanged" Margin="50,36,50,0" Grid.RowSpan="3">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#334155"/>
                    <Setter Property="Foreground" Value="#F1F5F9"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="T/r" Binding="{Binding Id}" Width="100" />
                <DataGridTextColumn Header="Ism" Binding="{Binding FullName}" Width="250"/>
                <DataGridTextColumn Header="Xonadon" Binding="{Binding Apartment}" Width="100"/>
                <DataGridTextColumn Header="Mashina raqami" Binding="{Binding CarNumber}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="O'chirish" Click="DeleteButton_Click"
                VerticalAlignment="Bottom" Width="100"
                HorizontalAlignment="Right" Background="#EF4444" Height="33"/>
    </Grid>
</Window>